{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "venv-activate",
            "type": "shell",
            "command": "source",
            "args": ["${workspaceFolder}/venv/bin/activate"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}